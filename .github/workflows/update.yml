name: Auto Fetch Stream

on:
  schedule:
    - cron: "*/5 * * * *"   # ทุก 5 นาที
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Fetch m3u8
        run: |
          curl -s "https://p0.cdn.vet/live/ch3/i/ch3i.m3u8?sid=b5yYTk0OTIwNTIzNDU1Mzg4ODczMQNzExN2JlODIzNWEzNGU4 "> latest.txt

      - name: Commit update
        run: |
          git config user.name github-actions
          git config user.email actions@github.com
          git add latest.txt
          git commit -m "update stream" || exit 0
          git push
